// Root-level ambient types live here.
// Type checking for each workspace is handled by apps/api/tsconfig.json and
// apps/web/tsconfig.json (run via `npm run typecheck`). This file exists so
// the repository-root `tsc -b --noEmit` check has a valid input.
export {};
